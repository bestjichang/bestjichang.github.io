# 本地测试与 GitHub Pages 部署完整教程

## 📋 目录

1. [前置准备](#前置准备)
2. [本地测试](#本地测试)
3. [发布到 GitHub Pages](#发布到-github-pages)
4. [常见问题](#常见问题)

---

## 前置准备

### 1. 确认环境

确保已安装以下工具：

- **Git**: 版本控制工具
- **Node.js**: 用于本地服务器（可选）
- **浏览器**: Chrome、Firefox、Edge 等

#### 检查 Git 是否已安装

打开命令提示符（cmd）或 PowerShell，运行：

```bash
git --version
```

如果显示版本号（如 `git version 2.x.x`），说明已安装。

如果未安装，请访问 [Git 官网](https://git-scm.com/download/win) 下载并安装。

#### 检查 Node.js 是否已安装

```bash
node --version
```

如果显示版本号，说明已安装。

如果未安装，请访问 [Node.js 官网](https://nodejs.org/) 下载并安装 LTS 版本。

### 2. 确认项目文件

确保你的项目目录 `d:\mycode\websites\jichangtuijian_github_io` 包含以下文件：

```
jichangtuijian_github_io/
├── index.html
├── about.html
├── css/
│   └── style.css
├── js/
│   ├── main.js
│   ├── theme.js
│   └── navigation.js
├── data/
│   └── airports.json
├── sitemap.xml
├── robots.txt
├── llms.txt
├── .gitignore
└── README.md
```

---

## 本地测试

### 方法一：使用 Python HTTP 服务器（推荐）

#### 步骤 1：打开命令提示符

1. 按 `Win + R` 键
2. 输入 `cmd` 并回车

#### 步骤 2：进入项目目录

```bash
cd /d d:\mycode\websites\jichangtuijian_github_io
```

#### 步骤 3：启动 HTTP 服务器

```bash
python -m http.server 8000
```

如果看到以下输出，说明服务器启动成功：

```
Serving HTTP on 0.0.0.0 port 8000 (http://0.0.0.0:8000/) ...
```

#### 步骤 4：在浏览器中访问

打开浏览器，访问：

```
http://localhost:8000
```

#### 步骤 5：测试功能

- ✅ 页面正常加载
- ✅ 深色/浅色模式切换正常
- ✅ 机场卡片显示正常
- ✅ 筛选功能正常
- ✅ 移动端响应式正常（调整浏览器窗口大小测试）
- ✅ 关于页面正常访问

#### 步骤 6：停止服务器

在命令提示符中按 `Ctrl + C` 停止服务器。

---

### 方法二：使用 Node.js serve

#### 步骤 1：安装 serve（只需安装一次）

```bash
npm install -g serve
```

#### 步骤 2：启动服务器

```bash
serve -p 8000
```

#### 步骤 3：在浏览器中访问

```
http://localhost:8000
```

---

### 方法三：直接打开 HTML 文件（最简单）

#### 步骤 1：在文件资源管理器中找到项目

打开 `d:\mycode\websites\jichangtuijian_github_io` 文件夹。

#### 步骤 2：双击 `index.html`

直接双击 `index.html` 文件，它会在默认浏览器中打开。

#### ⚠️ 注意事项

直接打开 HTML 文件可能存在以下限制：
- 某些浏览器可能阻止 `fetch` 请求（加载 `airports.json`）
- 路径可能不正确

**推荐使用方法一或方法二进行测试。**

---

## 发布到 GitHub Pages

### 步骤 1：创建 GitHub 仓库

1. 访问 [GitHub](https://github.com/) 并登录
2. 点击右上角的 `+` 按钮，选择 `New repository`
3. 填写仓库信息：
   - **Repository name**: `jichangtuijian.github.io`（必须与你的 GitHub 用户名匹配）
   - **Description**: `机场推荐 - GitHub Pages 博客`
   - **Public/Private**: 选择 `Public`（GitHub Pages 需要）
   - **Add a README file**: 取消勾选（我们已经有了）
   - **Add .gitignore**: 取消勾选（我们已经有了）
4. 点击 `Create repository`

### 步骤 2：初始化本地 Git 仓库

打开命令提示符，进入项目目录：

```bash
cd /d d:\mycode\websites\jichangtuijian_github_io
```

初始化 Git 仓库：

```bash
git init
```

### 步骤 3：添加文件并提交

```bash
git add .
```

提交更改：

```bash
git commit -m "Initial commit: GitHub Pages blog"
```

### 步骤 4：连接到 GitHub 仓库

```bash
git remote add origin https://github.com/bestvpsvpn/jichangtuijian.github.io.git
```

**注意**：将 `jichangtuijian` 替换为你的 GitHub 用户名。

### 步骤 5：推送到 GitHub

设置主分支名称为 `main`：

```bash
git branch -M main
```

推送到 GitHub：

```bash
git push -u origin main
```

如果提示输入用户名和密码：
- **Username**: 你的 GitHub 用户名
- **Password**: 你的 **Personal Access Token**（不是 GitHub 密码）

#### 如何获取 Personal Access Token

1. 访问 https://github.com/settings/tokens
2. 点击 `Generate new token` → `Generate new token (classic)`
3. 填写：
   - **Note**: `GitHub Pages Deployment`
   - **Expiration**: 选择过期时间（如 90 days）
   - **Select scopes**: 勾选 `repo`
4. 点击 `Generate token`
5. 复制生成的 token（只显示一次，请妥善保存）

### 步骤 6：启用 GitHub Pages

1. 访问你的仓库页面：https://github.com/jichangtuijian/jichangtuijian.github.io
2. 点击 `Settings` 标签
3. 在左侧菜单中找到 `Pages`
4. 在 `Build and deployment` 部分：
   - **Source**: 选择 `Deploy from a branch`
   - **Branch**: 选择 `main`，文件夹选择 `/ (root)`
5. 点击 `Save`

### 步骤 7：等待部署完成

- 部署通常需要 1-3 分钟
- 在 `Pages` 设置页面可以看到部署状态
- 部署完成后，会显示网站地址

### 步骤 8：访问你的网站

访问：

```
https://jichangtuijian.github.io/
```

---

## 常见问题

### Q1: `git push` 时提示 "Authentication failed"

**原因**: GitHub 不再支持密码登录，需要使用 Personal Access Token。

**解决方法**:
1. 生成 Personal Access Token（见上文步骤 5）
2. 再次执行 `git push`，使用 token 作为密码

### Q2: 本地测试时机场数据加载失败

**原因**: 直接打开 HTML 文件可能被浏览器安全策略阻止 `fetch` 请求。

**解决方法**: 使用 Python HTTP 服务器或 Node.js serve（见本地测试部分）。

### Q3: GitHub Pages 部署失败

**原因**: 可能是文件路径错误或配置问题。

**解决方法**:
1. 检查 `index.html` 是否在仓库根目录
2. 检查 GitHub Pages 设置中的分支是否正确
3. 查看 Actions 标签中的部署日志

### Q4: 网站显示 404

**原因**: 仓库名称不正确或未启用 GitHub Pages。

**解决方法**:
1. 确认仓库名称是 `你的用户名.github.io`
2. 确认已在 Settings → Pages 中启用部署

### Q5: 如何更新网站内容？

**步骤**:
1. 修改本地文件
2. 本地测试确认无误
3. 提交更改：
   ```bash
   git add .
   git commit -m "Update content"
   git push
   ```
4. 等待 GitHub Pages 自动部署

### Q6: 如何自定义域名？

**步骤**:
1. 在 GitHub 仓库的 Settings → Pages 中
2. 在 `Custom domain` 中输入你的域名
3. 按照提示配置 DNS 记录

---

## 📝 快速命令参考

```bash
# 进入项目目录
cd /d d:\mycode\websites\jichangtuijian_github_io

# 启动本地服务器（Python）
python -m http.server 8000

# 启动本地服务器（Node.js）
serve -p 8000

# 初始化 Git 仓库（只需一次）
git init

# 添加所有文件
git add .

# 提交更改
git commit -m "描述信息"

# 连接到远程仓库（只需一次）
git remote add origin https://github.com/用户名/仓库名.git

# 推送到 GitHub
git push -u origin main

# 查看状态
git status

# 查看提交历史
git log
```

---

## 🎉 完成！

现在你的 GitHub Pages 博客已经上线了！

访问 https://jichangtuijian.github.io/ 查看你的网站。

如有问题，请参考 [GitHub Pages 官方文档](https://docs.github.com/en/pages)。
